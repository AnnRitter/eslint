'use strict'

module.exports = {
  rules: {
    'fix': {
      meta: {
        fixable: "code"
      },
      create(context) {
        return {
          // логика правила
        }
      },
    },
  },
}